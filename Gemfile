source "https://rubygems.org"

# This installs the exact same Jekyll + plugin versions GitHub Pages uses,
# so what you see locally matches what deploys.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"
